package com.unicorn.backend.startup;

/**
 * Enum representing the status of a startup.
 */
public enum StartupStatus {
    ACTIVE, // Startup is live and visible
    APPROVED // Alias for ACTIVE (backwards compatibility)
}
