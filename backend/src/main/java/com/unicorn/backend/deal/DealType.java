package com.unicorn.backend.deal;

/**
 * Enum representing the type of investment deal.
 */
public enum DealType {
    SEED,
    PRE_SEED,
    SERIES_A,
    SERIES_B,
    SERIES_C,
    BRIDGE,
    CONVERTIBLE_NOTE,
    SAFE,
    EQUITY,
    DEBT,
    GRANT,
    OTHER
}
